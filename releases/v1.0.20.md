# JuHex Binary CLI v1.0.20

Prepared release notes for GitHub Releases.

## Native Packages

Upload these files as release assets for tag `v1.0.20`:

- `juhex-native-win-x64-1.0.20.zip`
- `juhex-native-macos-arm64-1.0.20.tar.gz`
- `juhex-native-macos-x64-1.0.20.tar.gz`
- `juhex-native-linux-x64-baseline-glibc-1.0.20.tar.gz`
- `juhex-native-linux-x64-musl-baseline-1.0.20.tar.gz`
- `juhex-native-linux-arm64-musl-1.0.20.tar.gz`
- `SHA256SUMS.txt`

## Verification

Native release verification completed locally before publishing:

```text
Native release verification: 6 verified, 0 pending, 0 failed
- win-x64: verified, smoke=skipped
- linux-x64-baseline-glibc: verified, smoke=skipped
- linux-x64-musl-baseline: verified, smoke=skipped
- linux-arm64-musl: verified, smoke=skipped
- macos-x64: verified, smoke=skipped
- macos-arm64: verified, smoke=skipped
```

Windows native archive structure and release checks passed on the build host. Linux and macOS execution smoke was skipped on the Windows build host; archive, checksum, manifest, payload, offline docs, install script, and MCP example checks passed.

This release includes the compiled `knowledge-refined` index: 226 Markdown files generated 4582 knowledge chunks.

## macOS Gatekeeper Notice

This macOS package has not been Developer ID signed or Apple notarized. If macOS says `juhex` is damaged or cannot be opened, remove quarantine from the extracted release directory or installed directory, then run `juhex --version` again.

The macOS installer prints exact commands for the current install paths.

## Checksums

See `SHA256SUMS.txt` in this release and `checksums/v1.0.20/SHA256SUMS.txt` in this repository.
